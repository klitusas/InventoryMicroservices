package com.products;

import java.util.Set;

import org.springframework.data.rest.core.config.Projection;

@Projection(name="inlineProducts", types={Unit.class})
public interface InlineUnits {
	String getName();
	String getLocation();
	Set<Product> getProducts();
}

